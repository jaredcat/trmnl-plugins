{
  "name": "trmnl-plugins",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "typescript": "^5.9.3"
  }
}
